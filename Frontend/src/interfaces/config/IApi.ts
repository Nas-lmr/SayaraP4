/**
 * Type of apiConfig constant
 */
export interface IApi {
  private: {
    login: string;
    persist: string;
    logout: string;
    register: string;
    postTrajet: string;
    searchTrajet: string;
  };
}
